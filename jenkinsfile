pipeline {
    agent any

    stages {
        stage('Run Local Code') {
            steps {
                script {
                    // Ensure you have Python and necessary libraries installed
                    sh 'python3 -m pip install boto3'
                    sh 'python3 athena_connection_s3.py'
                }
            }
        }
    }
}